import PageHeader from '../components/ui/PageHeader';

function ChatPage() {
  return (
    <section>
      <PageHeader
        title="Chat Assistant"
        description="Chat conversation layout, suggestion chips, and composer area are prepared to be implemented next."
      />
    </section>
  );
}

export default ChatPage;
